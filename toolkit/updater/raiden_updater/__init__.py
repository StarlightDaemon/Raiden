"""RAIDEN Updater MVP — local CLI for managed-core updates.

This package implements the first practical updater for RAIDEN Instance
managed-core updates sourced from Edict packages. The package manifest,
instance metadata, installed baseline, and core version comparison rules now
reflect current local CLI updater canon.
"""

__version__ = "0.1.0"
